# Frontend Environment Variables (VITE_ prefix for client-side access)
# SECURITY NOTE: DO NOT use VITE_GEMINI_API_TOKEN in production!
# All Gemini API calls MUST go through the backend proxy for security.
# The VITE_GEMINI_API_TOKEN is deprecated and should not be used.

# Backend Server Configuration
NODE_ENV=development
# In development: binds to 127.0.0.1 (localhost only)
# In production: set HOST to 0.0.0.0 or specific IP to allow network access
HOST=127.0.0.1
PORT=3001

# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:5173
CLIENT_URL=http://localhost:5173

# Session Configuration
# IMPORTANT: Change this to a long, random string in production
SESSION_SECRET=change-this-secret-in-production-use-a-long-random-string

# Gemini API Configuration (SERVER-SIDE ONLY)
# This key is used by the backend proxy to make secure API calls
# NEVER expose this key in client-side code or environment variables with VITE_ prefix
# Requirements: 5.1, 5.2, 5.4 - Backend API security
GEMINI_SERVICE_TOKEN=your_gemini_api_key_here

# JWT Configuration (for authentication)
JWT_SECRET=your-secret-key-change-in-production
